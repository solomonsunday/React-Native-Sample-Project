import axios from 'axios';
import { getToken } from '../../api/index';

// const actionUrl = 'https://apis.woozeee.com/api/v1/entry-comments';

// export default sendComment = async (commentContent) => {
//   console.log(commentContent);
//   const config = {
//     method: 'POST',
//     headers: {
//       Accept: 'application/json',
//       'Content-Type': 'application/json',
//       Authorization: `${await getToken()}`,
//     },
//     body: JSON.stringify(commentContent),
//   };
//   const res = await axios.post(actionUrl, commentContent, config);
//   console.log(res);
//   return res;
// };
